diff --git a/server.js b/server.js
--- a/server.js
+++ b/server.js
@@
     posts_por_dia INTEGER DEFAULT 1,
     ultimo_post DATETIME,
+    ultimo_slot TEXT,
     created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
@@
   // seed admin user
+  db.run("ALTER TABLE post_schedules ADD COLUMN ultimo_slot TEXT", () => {});
   const email = 'wolfofdown@gmail.com';
@@
-    let corFoco = corFocoAceitavel(varFoco) ? varFoco : null;
-    if (!corFoco && corFocoAceitavel(ctaBtnHex)) corFoco = ctaBtnHex;
-    if (!corFoco && corFocoAceitavel(primaryBtnHex)) corFoco = primaryBtnHex;
-    if (!corFoco && varFoco) corFoco = varFoco; // queda: usa a var mesmo assim
+    // Prioriza a cor realmente usada no CTA visível. Variáveis como --accent-orange
+    // podem existir no site sem serem a cor principal de conversão.
+    let corFoco = corFocoAceitavel(ctaBtnHex) ? ctaBtnHex : null;
+    if (!corFoco && corFocoAceitavel(primaryBtnHex)) corFoco = primaryBtnHex;
+    if (!corFoco && corFocoAceitavel(varFoco)) corFoco = varFoco;
+    if (!corFoco && varFoco) corFoco = varFoco; // queda: usa a var mesmo assim
@@
 function humanizarTexto(texto) {
@@
   return resultado;
 }
+
+function normalizarKeywordPrincipal(valor, local) {
+  let termo = String(valor || '').replace(/\s+/g, ' ').trim();
+  termo = termo.replace(/([A-Za-zÀ-ÿ])(?=\d)/g, '$1 ');
+  termo = termo.replace(/\s+/g, ' ').trim();
+  termo = termo.replace(/\s+em\s+(?:emergencial|urgente|24h|24\s*horas)\b.*$/i, '').trim();
+  return termo.slice(0, 140);
+}
+
+function escaparRegex(texto) {
+  return String(texto || '').replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
+}
@@
-          const termo = kw ? (kw.variacao || kw.termo) : (client.nicho || 'serviços especializados');
-          const termoBase = kw ? kw.termo : termo;
+          const termo = kw ? (kw.variacao || kw.termo) : (client.nicho || 'serviços especializados');
+          const termoBase = normalizarKeywordPrincipal(kw ? kw.termo : termo, client.localizacao);
@@
-3. H2s: Devem conter termos de intenção de busca reais que o usuário pesquisaria: "quanto custa", "como funciona", "perto de mim", "urgence", "24 horas", "orçamento".
+3. H2s: Devem conter termos de intenção de busca reais que o usuário pesquisaria: "quanto custa", "como funciona", "perto de mim", "urgente", "24 horas", "orçamento".
@@
-- Corpo do texto: 3 a 5 vezes em <strong>negrito</r
+- Corpo do texto: 3 a 5 vezes em <strong>negrito</strong>.
@@
               .replace(/^```[a-zA-Z0-9]*\s*\r?\n?/i, '')
               .replace(/\r?\n?```\s*$/i, '')
               .trim();
+
+            // A IA às vezes repete o próprio título como primeiro parágrafo.
+            if (titulo) {
+              const tituloRegex = new RegExp(`^\\s*${escaparRegex(titulo)}\\s*\\r?\\n+`, 'i');
+              conteudo = conteudo.replace(tituloRegex, '').trim();
+            }
+
+            // Se a IA ignorar HTML e devolver texto/Markdown, normaliza antes de salvar.
+            if (!/<(?:h2|h3|p|ul|ol|li)\b/i.test(conteudo)) {
+              conteudo = humanizarTexto(conteudo);
+            }
@@
-        // Verifica se o horário atual bate com o agendamento OU se estava previsto nos últimos 3 minutos
+        // Aceita no máximo 1 minuto de atraso. O slot exato abaixo impede duplicação.
         const matchesNow = horarios.includes(hhmm);
@@
-            if (diffMin > 0 && diffMin <= 3) {
+            if (diffMin > 0 && diffMin <= 1) {
@@
-        // Evita disparar duas vezes para o mesmo agendamento nos últimos 3 minutos
-        if (sched.ultimo_post) {
-          const ultimo = new Date(sched.ultimo_post.endsWith('Z') ? sched.ultimo_post : sched.ultimo_post + 'Z');
-          const diffMin = (now.getTime() - ultimo.getTime()) / 60000;
-          if (diffMin < 3) continue;
-        }
+        const slotHora = matchesNow ? hhmm : horarios.find(h => {
+          const [hHour, hMin] = h.split(':').map(Number);
+          const diffMin = (hour * 60 + minute) - (hHour * 60 + hMin);
+          return diffMin > 0 && diffMin <= 1;
+        });
+        if (!slotHora) continue;
+        const slotKey = `${isoDate} ${slotHora}`;
+        if (sched.ultimo_slot === slotKey) continue;
@@
-        db.run('UPDATE post_schedules SET ultimo_post = CURRENT_TIMESTAMP WHERE id = ?', [sched.id]);
+        db.run('UPDATE post_schedules SET ultimo_post = CURRENT_TIMESTAMP, ultimo_slot = ? WHERE id = ? AND (ultimo_slot IS NULL OR ultimo_slot != ?)', [slotKey, sched.id, slotKey], function(updateErr) {
+          if (updateErr || this.changes !== 1) return;
@@
         } else {
           gerarPostAutomatico(sched.client_id)
             .then(r => console.log(`[CRON] ✅ Post publicado: \"${r.titulo}\" para ${r.client_name}`))
             .catch(e => console.error(`[CRON] ❌ Falha:`, e.message));
         }
+        });
*** End Patch
